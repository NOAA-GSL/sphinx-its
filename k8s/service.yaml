apiVersion: v1
kind: Service
metadata:
  name: sphinx-its-svc
  namespace: sphinx-its
spec:
  type: NodePort
  selector:
    app: sphinx-its
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
